# --- Dependencies (Thư viện) ---
node_modules/
/backend/node_modules
/frontend/node_modules

# --- Environment Variables (QUAN TRỌNG: BẢO MẬT) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Đảm bảo ignore file .env cụ thể của từng folder
/backend/.env
/frontend/.env

# --- Backend Specifics ---
# Ignore các file ảnh/video do người dùng upload
/backend/uploads/*
# Giữ lại file .gitkeep để Git vẫn tạo thư mục uploads rỗng khi clone về
!/backend/uploads/.gitkeep

# Ignore các file SQL backup tạm thời (do backupCloudinary.js tạo ra)
/backend/*.sql
/backend/backup-*.sql

# --- Frontend Build Output ---
# Thư mục chứa code React sau khi build (production)
/frontend/build
/frontend/dist

# --- Logs (File ghi lỗi) ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log
pids
*.pid
*.seed
*.pid.lock

# --- System & Editor Files (Rác hệ điều hành/Editor) ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp